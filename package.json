{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm start --prefix app\" \"npm start --prefix cart\" \"npm start --prefix products\"",
    "shell": "npm start --prefix shell",
    "postinstall": "(cd shell && npm install); (cd app && npm install); (cd cart && npm install); (cd products && npm install)"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "engines": {
    "node": ">=14"
  }
}
